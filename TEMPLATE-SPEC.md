# 設定差し替え仕様

## 設定ファイル

実行時に読み込む設定は`diagnosis-template/config.js`だけである。新しい顧客診断では、このファイルの内容だけを差し替え、本体のHTML・CSS・JavaScriptは変更しない。

第2サンプルは`diagnosis-template/configs/work-style.js`にある。これを`config.js`へコピーすると、仕事の進め方チェックとして同じ画面遷移と採点処理が動作する。

## 必須設定

- `id`、`title`、`eyebrow`、`description`
- `resultTitle`、`resultDescription`
- `labels`：開始、次へ、戻る、結果、再回答
- `theme`：8色すべてを6桁の16進カラーで指定
- `axes`：重複しないキー、表示名、結果説明文
- `questions`：重複しないID、質問文、2件以上の選択肢
- 各選択肢：重複しないID、表示文、1つ以上の有効な軸への配点

## 検証規則

- 軸キーは英小文字で始まる識別子
- 配点は0以上の安全な整数で、選択肢ごとに1点以上を含む
- 未定義軸への加点を拒否
- 全質問の回答が揃うまで結果を計算しない
- 最高軸情報は互換性のため計算結果に残すが、分類・閾値として画面表示しない

## 画面と状態

- 開始、質問、結果の3画面
- 質問は1問ずつ表示し、進捗を表示
- 戻る操作でも回答を保持
- 結果は全軸の点数、割合、設定された説明文を表示
- 再回答で回答状態を消去して開始画面へ戻る

## 実行境界

- 外部依存、ビルド、外部通信、データ保存なし
- 既存の`integration.js`は過去資産として残すが、本体から読み込まず呼び出さない
- 分類、閾値、商品推薦、個人情報収集は実装しない
