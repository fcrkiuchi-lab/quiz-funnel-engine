# 仕様：設定差し替え型の診断テンプレート

## 位置付け

- `diagnosis-template/` は再利用用の独立サンプル
- 公開中の3点割合計算機の仕様、画面、計算処理は変更しない
- HTML、CSS、JavaScriptだけで動作し、外部依存とビルドを必要としない

## 設定データ

`diagnosis-template/config.js` で次を定義する。

- 診断タイトルと説明
- 1件以上の結果軸（重複しないキーと表示名）
- 1件以上の設問（重複しないIDと設問文）
- 各設問に2件以上の選択肢（設問内で重複しないID、表示文、加点先）
- 任意連携の有効・無効、送信先、診断ID

加点値は0以上の安全な整数とし、各選択肢は少なくとも1つの結果軸へ1点以上を加点する。定義していない結果軸への加点は拒否する。

## 入力と採点

- 全設問で選択肢を1つ選ぶ
- 未回答、存在しない選択肢、存在しない設問への回答を拒否する
- 選択した各選択肢の加点値を結果軸ごとに合計する
- 合計点に占める各結果軸の割合を小数第1位で表示する
- 最高点の結果軸を返し、同点の場合は同点の全結果軸を返す
- 丸めにより表示割合の合計が100.0%にならない場合がある旨を表示する

## 任意のn8n連携境界

- 既定値は無効であり、無効時は通信処理を呼び出さない
- `enabled: true` とHTTPSの送信先を両方設定した場合だけ、結果表示後にPOSTする
- 送信項目はスキーマ版、診断ID、点数、割合、最高点の軸、回答済み設問数に限定する
- 個々の回答、個人情報、秘密情報は送信しない
- 認証、再送、保存、VPS、n8nワークフロー構築はこの境界の責務外とする

## 自動テストの合格条件

- サンプル設定を検証できる
- 設定データの差し替えで加点先が変わる
- 点数、割合、単独最高点、同点最高点が正しい
- 未回答と不正な設定を拒否する
- 連携無効時に通信しない
- 連携有効時はモック送信先へ規定の項目だけをPOSTする
- 公開中の割合計算機の既存7テストが引き続き合格する

## 範囲外

- 公開中の割合計算機への組み込み
- 商品情報と商品推薦
- 回答または結果の保存
- メール、LINE、CRM、注文、集計との連携
- n8n、VPS、SSHの構築または調査
- 外部API、外部ライブラリ、ビルドツールの追加
